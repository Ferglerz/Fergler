// Compression Constants Module
// All constants specific to compression processing
// Dependencies: None

@init

// === COMPRESSION CONSTANTS ===

MAX_GR_RANGE_DB = 30.0; // For normalization calculations

// Compression Lookup Table Configuration
COMP_LUT_MIN_DB = -120;       // Minimum dB value in lookup table (extends below graph range to capture expansion)
COMP_LUT_MAX_DB = 20;         // Maximum dB value in lookup table (allows headroom for makeup)
COMP_LUT_RANGE_DB = 140;      // Total range (20 - (-120) = 140dB)
COMP_LUT_GRANULARITY = 0.25;  // 0.25dB steps
COMP_LUT_SIZE = 560;          // 140dB / 0.25dB = 560 entries

